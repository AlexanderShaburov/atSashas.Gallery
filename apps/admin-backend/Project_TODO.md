# TODO when Blocks/Streams are implemented:
# - scan blocks directory
# - identify blocks containing this art_id
# - scan streams including those blocks
# - return dependencies
# - block deletion if required

## Things to do, but not at the moment:

_As complete mark with ✅ symbol please_

1. Ensure the fields in form corresponding to undefined values don't have (if possible) default values.

2. Make Price parameter have three states:

- price - price is set;
- undefined - price not set;
- null - price for request;

3. Add _"add technique"_ button on the form and change technique attribute to string[].
